unit ufrmComponenteSidebar;

interface

uses
  System.SysUtils, System.Types, System.UITypes, System.Classes, System.Variants, 
  FMX.Types, FMX.Graphics, FMX.Controls, FMX.Forms, FMX.Dialogs, FMX.StdCtrls,
  FMX.Layouts;

type
  TComponenteSidebar = class(TFrame)
    lytContainer: TLayout;
  private
    { Private declarations }
  public
    class function New:TComponenteSidebar  ;
    function component:TFmxObject;
  end;

implementation

{$R *.fmx}

{ TComponenteSidebar }

function TComponenteSidebar.component: TFmxObject;
begin
Result:=lytContainer
end;

class function TComponenteSidebar.New: TComponenteSidebar;
begin
      result := self.Create(nil);
end;

end.
